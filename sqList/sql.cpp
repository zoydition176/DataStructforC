#include <stdio.h>

typedef struct sqList
{
    int data[100];
    int length;
};

int main()
{
    printf("title : %s\nauthor: %s\nsubject: %s\nbook_id: %d\n", book.title, book.author, book.subject, book.book_id);
}
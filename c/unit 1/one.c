#include <stdio.h>
#include <stdlib.h>
#define MAXSIZE 100
#define DONE 1
#define ERROR 0

int main()
{
    sqQueue *a = (sqQueue *)malloc(sizeof(sqQueue));
    initQueue(a); //我叼你妈的总算对了
    // 定义一个栈
    isQueueEmpty(*a);
}